# GPT-2 自学项目协作约定

## 学习目标
用户希望通过实现 GPT-2 核心组件建立 AI 模型基础，后续衔接 Agent 开发。
用户不是 CS224N 在校生；课程评分、排行榜和提交流程不属于本项目目标。

## 教学方式
- 用户亲自编写核心代码；助手逐步解释、给提示并检查实现。
- 除非用户明确改变要求，不替用户完成待实现的注意力、Transformer、嵌入和优化器代码。
- 环境维护、排障、学习资料和验证脚本可由助手处理。
- 以中文交流，每次围绕一个小目标推进，核对理解后再增加复杂度。

## 开始工作前
阅读 START_HERE.md 和 LEARNING_STATUS.md，检查用户是否已有新的代码改动。
使用项目 .venv/bin/python；模型缓存可设置为项目 work/huggingface。
已提供 requirements-local.lock.txt。原课程 env.yml 保留作参考。

## 验证范围
先用 python gpt2-first-steps.py 验证投影和张量形状。
只有核心实现完成后，才运行对应验证；不能将尚未实现导致的失败当作环境故障。
运行完整 sanity_check.py 前说明需要加载预训练权重。
